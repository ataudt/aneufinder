useDynLib(aneufinder)

# Export all names
# exportPattern(".")
export(
bed2binned,
bam2binned,
bedGraph2binned,

find.aneuploidies,

plot.distribution,

univariate2bed,
binned2wiggle,
multivariate2bed,
binned2GRanges,
hmm2GRanges,
bed2GRanges,

mixedsort,
mixedorder
)

# Import all packages listed as Imports or Depends
import(
	GenomicRanges, IRanges
)
