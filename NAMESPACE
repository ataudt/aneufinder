useDynLib(aneufinder)

# Export all names
# exportPattern(".")
export(
bed2binned,
bam2binned,
bedGraph2binned,

find.aneuploidies,

plot.distribution
)

# Import all packages listed as Imports or Depends
import(
	GenomicRanges, IRanges
)
